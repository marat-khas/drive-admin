import { FilterState } from './types';

export const FilterStateDefault: FilterState = {
    limit: 10,
    page: 1,
    dateFrom: null,
    dateTo: null,
    carId: null,
    cityId: null,
    categoryId: null,
};
